<form method="post" action="{{ path('comment_delete', {'comment': comment.id, 'slug': program.slug, 'season': season.id, 'episode_slug': episode.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment.id) }}">
    <button class="btn">Delete</button>
</form>
