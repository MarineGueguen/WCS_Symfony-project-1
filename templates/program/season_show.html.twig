{% extends 'base.html.twig' %}

{% block title %}Série #{{ program.id }}{% endblock %}

{% block body %}
    <div class="media">
        <img class="align-self-start mr-3" src="{{ program.poster }}" alt="{{ program.title }} poster">
        <div class="media-body">
            <h1 class="mt-0">Season {{ season.number }} of {{ program.title }}</h1>
            <p>{{ program.synopsis }}</p>
            <p>List of episodes :</p>
            {% for episode in episodes %} 
            <p><a href="{{ path('program_episode_show', { slug: program.slug, season: season.id, episode_slug: episode.slug }) }}">Episode {{ episode.number }} : {{ episode.title }}</a></p>
            {% endfor %}
        </div>
    </div>

    <a href="{{ path('program_show', { slug: program.slug }) }}">
        Retour à la fiche de la série
    </a>
{% endblock %}
