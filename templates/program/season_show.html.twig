{% extends 'base.html.twig' %}

{% block title %}Série #{{ season.program.id }}{% endblock %}

{% block body %}
    <div class="media">
        <img class="align-self-start mr-3" src="{{ season.program.poster }}" alt="{{ season.program.title }} poster">
        <div class="media-body">
            <h1 class="mt-0">Season {{ season.number }} of {{ season.program.title }}</h1>
            <p>{{ season.program.synopsis }}</p>
            <p>List of episodes :</p>
            {% for episode in episodes %} 
            <p>Episode {{ episode.number }} : {{ episode.title }}</p>
            {% endfor %}
        </div>
    </div>
{% endblock %}
